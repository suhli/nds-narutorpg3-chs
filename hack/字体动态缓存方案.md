# 字体动态缓存方案

更新时间：2026-05-27
状态：第二版按字符码替换已验证，稳定性复核通过，进入动态缓存设计

## 1. 当前结论

原字体绘制链路已经动态确认：

```text
0208913C  文本绘制入口
0208671C  字符编码 + mode 查表，返回 glyph 源地址
02089190  调用复制函数，R0=源 glyph，R1=目标缓存，R2=大小
```

当前标题/菜单样本走 `mode=1`：

```text
chr1x2_base = 0x06881C00
unit_size   = 0x40
```

## 2. 最小验证目标

第一版不做完整中文字库缓存，只验证：

- glyph 复制函数能否接受非原 VRAM 字库地址作为 `R0`。
- `02089190` 前替换 `R0` 后，画面是否显示自定义 glyph。

如果成立，后续可把 `R0` 指向“按需准备的 glyph cache”，从而避开全量中文字模常驻 VRAM。

## 3. 第一版 Hook

Patch 点：

```text
02089190: bl 020087BC
```

改为：

```text
02089190: bl hook
```

hook 行为：

```text
if R0 == 0x06881C00:
    R0 = custom_glyph_data
b 020087BC
```

实际落点：

- `02089190` 改为 `BL 0x0207411C`。
- `0x0207411C` 位于 ARM9 主程序零区，不占 overlay BSS。
- `0x06881C00` 是字符 `0x8140` 在 `font_1x2.tbl` 中返回的源地址。
- `custom_glyph_data = 0x0207413C`。
- `R1/R2` 保持原样。

已验证：

```text
hook 入口:
R0=0x06881C00
R1=0x02292A40
R2=0x00000040
LR=0x02089194

进入原复制函数 020087BC:
R0=0x0207413C
R1=0x02292A40
R2=0x00000040
LR=0x02089194
```

结论：复制函数接受非原 VRAM 字库地址作为 glyph 源。这条路可以继续扩展成“按需准备 glyph 数据，再替换 `R0`”的动态缓存方案。

## 5. 第二版：按字符码替换

新增测试 ROM：

```text
rom/test_vram_font_char_hook_probe.nds
```

新增脚本：

```text
tools/patch_vram_font_char_hook_probe.py
```

新增两个 hook：

```text
0208914C -> 0207411C
02089190 -> 02074140
```

`0207411C` 保存当前字符码：

```text
mov r6, r1
*(u32*)0x020741C0 = r1
```

`02074140` 在复制前读取当前字符码：

```text
if (*(u32*)0x020741C0 == 0x82CD):
    R0 = 0x02074180
b 020087BC
```

MCP 已验证：

```text
current_char=0x82CD
R0=0x02074180
PC=0x020087BC
R1=0x02292B40
R2=0x00000040
```

结论：

- 当前字符码可以从 `0208914C` 保存。
- `02089190` copy hook 可以按字符码选择 glyph 源。
- 这已经接近正式动态缓存所需的控制面：`char_code -> glyph_source`。

风险：

- 此前记录的 `running=0 ARM9_PC=0x0208AA44 / ARM7_PC=0x00000020` 可通过 `nds_resume` 继续运行，不能作为崩溃证据。
- no-op 复核已经完成：保存字符 hook 稳定，copy hook 框架稳定。
- “替换为原 glyph 拷贝”版本已经复核：`0x02074180` 与 `0x06882280` 数据一致，清除断点后可继续运行。
- 下一步进入动态 glyph 缓存设计，重点转为中文 glyph 数据来源、映射表和缓存槽管理。

## 6. 稳定性复核

保存字符 no-op：

```text
rom/test_vram_font_save_char_noop_probe.nds
0207411C 命中，R1=0x8140
放行后 current_char=0x82E9
running=1
```

copy hook no-op：

```text
rom/test_vram_font_copy_noop_probe.nds
02074140 命中，current_char=0x8140
R0=0x06881C00
放行后 current_char=0x82E9
running=1
```

结论：

```text
保存当前字符码的路径可用。
copy hook 尾调用 020087BC 的路径可用。
当前不稳定集中在命中 0x82CD 后把 R0 替换为自定义 glyph 数据。
```

## 4. 后续正式方向

第二版稳定后，再扩展为：

- 在 `0208671C` 或 `02089190` 处识别中文字符。
- 从 ROM/RAM 字模仓库按需取 glyph。
- 将 glyph 放入小型缓存区。
- 返回/替换为缓存区地址。
- 维护字符到缓存槽的映射和淘汰策略。

需要同时考虑：

- `.chr` 容量。
- `.tbl` 容量。
- 1x1/1x2 mode。
- 同屏复用和文本刷新。

## 7. 多字符小表 probe

新增脚本：

```text
tools/patch_vram_font_multi_char_hook_probe.py
```

新增 ROM：

```text
rom/test_vram_font_multi_char_hook_probe.nds
rom/test_vram_font_multi_char_8140_probe.nds
```

运行时已验证：

```text
current_char=00008140
R0=0x020741A0
R1=0x02292A40
```

含义：`02089190` 前的 hook 已经可以从“单字符 if”扩展为“小表分流”，并把实际出现的字符路由到独立 RAM glyph 源。下一步需要继续验证第二个字符项，并把硬编码比较改成正式映射表查找。

复核更新：

```text
tools/sample_vram_font_chars_mcp.py
```

已经采到稳定序列：

```text
0x8140, 0x8140, 0x8140, 0x8140, 0x82CD, 0x82B6, 0x82DF
```

默认双字符 ROM 已验证：

```text
0x82CD -> R0=0x020741A0
0x82DF -> R0=0x020741E0
```

因此“小表分流”已从单项验证推进到双项验证。下一步进入循环查表原型。

## 8. 表驱动查找原型

新增脚本：

```text
tools/patch_vram_font_table_hook_probe.py
```

新增 ROM：

```text
rom/test_vram_font_table_hook_probe.nds
```

`02074140` 已改为循环查表：

```text
table entry:
u32 char_code
u32 glyph_addr
```

当前测试表：

```text
0x82CD -> 0x02074200
0x82DF -> 0x02074240
```

MCP 运行时验证：

```text
current_char=0x82CD  R0=0x02074200
current_char=0x82DF  R0=0x02074240
```

结论：动态缓存所需的 `char_code -> glyph_source` 表驱动控制面已经成立。下一步需要解决 glyph 数据和映射表从 NitroFS/普通 RAM 预加载的问题，而不是继续把测试 glyph 写死在 ARM9 空洞。

## 9. NitroFS 文件预加载到 RAM 原型

新增脚本：

```text
tools/patch_vram_font_file_preload_probe.py
```

新增 ROM：

```text
rom/test_vram_font_file_preload_probe.nds
```

新增 NitroFS 文件：

```text
font/chs_probe.bin
```

文件结构：

```text
u32 entry_count
entry:
  u32 char_code
  u32 glyph_offset
glyph bytes
```

当前 hook：

```text
020869E0 -> 020741A0  在字体初始化尾部加载 chs_probe.bin
02089190 -> 02074140  从 RAM 文件结构查表并替换 R0
```

运行时验证：

```text
chs_data_ptr  = 0x02282F40
chs_data_size = 0x000000A0
0x82CD -> R0=0x02282F60
0x82DF -> R0=0x02282FA0
```

结论：自定义字模和映射表可以放在 NitroFS 文件中，启动时预加载到普通 RAM，绘制时只把 `R0` 指向 RAM 中对应 glyph。这样已经绕开了“完整中文字库常驻 VRAM”的核心限制。

## 10. RAM 预加载容量判断

新增压力测试脚本：

```text
tools/run_font_preload_size_sweep.py
```

验证对象仍是 `font/chs_probe.bin`，但把文件总大小扩大到不同档位。文件头和两个测试 glyph 保持一致，后续只做填充，用于观察整文件预加载对 RAM 的压力。

关键结果：

```text
512K/896K/960K/992K/1008K 可进入文本绘制采样。
1008K: 82CD -> R0=0x02286B80, 82DF -> R0=0x02286BC0。
1024K: 分配后推进到文本流程失稳，未取得正常绘制样本。
1392K+: chs_data_ptr=0，连续 RAM 分配失败。
```

判断：

- `0207F80C` 不会自动把大文件分页加载；当前用法是整文件连续加载到 RAM。
- 文件太大时会出现 `chs_data_size` 被写入但 `chs_data_ptr=0` 的失败形态。
- 即使拿到 pointer，接近 1MB 时也可能压缩游戏后续运行空间。
- 正式方案不能把完整中文字模设计成单个大文件常驻 RAM。

映射表容量判断：

```text
8 bytes/entry 时：
10000 字约 80KB
20000 字约 160KB
```

映射表本身不太可能先超过 RAM；优先需要控制的是 glyph 数据。1x2 glyph 当前是 `0x40 bytes/glyph`，10000 个 glyph 约 640KB。后续应采用：

```text
常驻 map/header
按页或按块加载 glyph 数据
小型 RAM staging buffer
当前画面 VRAM glyph cache
```
