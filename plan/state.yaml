current_plan:
  id: vram-font-bypass
  title: 绕过 VRAM 全量字体加载方案
  document: plan/vram-font-bypass.md
  status: in_progress
current_stage:
  id: runtime-observation
  name: 运行时确认关键地址和数据流
  status: in_progress
stage_cache:
  dir: plan/cache/vram-font-bypass
  documents:
    - plan/cache/vram-font-bypass/plan-draft.md
    - plan/cache/vram-font-bypass/static-render-chain.md
    - plan/cache/vram-font-bypass/runtime-observation.md
updated_at: "2026-05-27"
notes:
  - 新对话先读本文件，再读取 current_plan.document。
  - 具体计划使用 Markdown 文档组织，state 只做恢复入口和当前进度指针。
  - 静态链路已完成，运行时已确认字体初始化入口和 DAT_020B73E0 字段。
  - 当前阻塞点是正文/菜单字体绘制断点需要输入推进或 MCP 输入能力。
