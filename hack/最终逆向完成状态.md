# 最终逆向完成状态

日期：2026-06-12

状态：完成。

用户已验证 `rom/narutorpg3_chs_patcher_v36_no_pre03_spaces.nds` 当前无已知问题。以下逆向结论作为最终回写规则保留。

## 已完成结论

- 字体：双模式中文字库方案可用，当前最终 ROM 沿用已验证字体承载与绘制 hook。
- 文本：剧情、系统、菜单、战斗、道具/忍术说明等文本已进入最终合并数据。
- 菜单 overlay：运行时使用的 overlay 文本副本已纳入最终产物。
- 固定布局：装备、状态、物品、忍术、用户菜单等固定布局文本按原始槽位或模板对齐，不再用普通长文本规则覆盖。
- 控制符：`{CTRL_xxxx}` 视为二进制控制参数，回写时按记录类型保留原有顺序和槽位语义。
- `{CTRL_0001}`：在普通对白中作为换页/换行类控制符处理，不能简单删除或替换为空字符。
- `03 00`：文本终止符必须保留，且同一记录内不能由填充或拼接引入额外终止符。
- 事件脚本 `msg/fld/evt/`：不能在 `03 00` 后使用 `00` 尾填充，否则会跳过连续事件对白。
- v36 规则：事件脚本 message 使用 `译文 + 03 00 + 全角空格尾填充`，且不在最终 `03 00` 前留下全角空格填充。
- 非事件普通 message：沿用已验证的早停终止符与尾填充规则。

## 最终交付

- 单数据文件：`patcher/narutorpg3_chs_v36.json`
- 单 CLI：`patcher/patcher.py`
- 最终 ROM SHA256：`B29FEA1B5B7BBD5E2010BD5AF1262676B6B71CB1D6E126847BECCB9A71954BB9`

后续如果发现新运行时问题，应作为新问题从最终 v36 产物继续排查，而不是恢复历史拆分脚本流程。
