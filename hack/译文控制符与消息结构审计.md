# 译文控制符与消息结构审计

## 结论

全量写回不能只比较原始 `jp_text` 与译文控制符。部分 `.m` 消息行的开头含脚本结构前缀，dump 会把这些字节误识别成 `{CTRL_xxxx}` 或 ASCII 字符。

当前审计规则以“调整后原文”为基准：

- `81 75` 开引号前的消息结构字节只保留在 ROM，不进入译文；
- `xx 00 + SJIS正文` 形态中，`xx 00` 是消息前缀，正文从后面的 SJIS 字节开始；
- 固定槽末尾的 `00` padding 不要求译文写成 `{CTRL_0000}`；
- 译文必须保留调整后正文中的真实 `{CTRL_xxxx}` 顺序；
- 文本变短时仍由写回层在结束符前补 `81 40`，不提前移动 `03 00`。

## 工具

```text
tools/audit_structsafe_translations.py
```

最终报告：

```text
plan/cache/text-writeback-smoke/translation-struct-audit-report.json
plan/cache/text-writeback-smoke/translation-struct-mismatches.tsv
plan/cache/text-writeback-smoke/encoded-preview-struct-adjusted.tsv
```

最终统计：

```text
checked_rows=5843
auto_adjusted_rows=1009
manual_override_rows=5
after_control_mismatch_rows=0
overflow_rows=0
missing_char_rows=0
```

## 写回联动

`tools/build_text_writeback_smoke_rom.py` 已同步支持两类消息前缀：

- `preserve_prefix_before_open_quote`
- `preserve_prefix_before_sjis_text`

这样报告中的调整可以直接用于 ROM 构建，不会在构建阶段再次覆盖消息前缀。
