# 菜单文本批量翻译与回写备忘

## 结论

标题菜单中文已显示，说明 overlay 菜单文本可以复用当前中文码表和 1x2 动态字体路径。之前方块字的关键原因不是字模文件完全错误，而是延迟缓存策略在标题菜单首次绘制时不能立即提供缺失字形。

当前可工作的测试 ROM 使用 `immediate_copy_on_1x2_miss_probe`。它验证了 1x2 首次 miss 同步填缓存的方向，但还不是完整正式字体补丁，1x1 路径仍需后续收敛。

## 菜单资产处理流程

1. 扫描 ARM9 和 overlay 中的 CP932 固定槽字符串：

```text
tools/scan_menu_overlay_strings.py
text/menu/overlay_menu_candidates.tsv
text/menu/overlay_menu_scan_report.json
```

2. 从扫描候选中只选择连续菜单/提示表区批量处理：

```text
overlay_0000 offset >= 0x2F6AC
overlay_0001 offset >= 0x1EC60
overlay_0002 offset >= 0x128B8
overlay_0003 offset >= 0x42A60
overlay_0004 offset >= 0x5270
```

3. 菜单翻译单独维护，不混进正文翻译表：

```text
tools/prepare_overlay_menu_translations.py
text/menu/overlay_menu_translations.tsv
text/menu/overlay_menu_translation_report.json
```

当前批量菜单表：

```text
selected_rows=278
unique_keys=206
ready=278
missing_font_chars=0
```

## 翻译原则

- 按去首尾空格后的 `jp_key` 去重翻译，同一短语复用同一译文。
- 菜单译文优先短、直观、适配固定槽容量。
- 保留 `%s`、全角数字、`／`、`：`、`？` 等格式符和 UI 符号语义。
- 普通菜单项不手工补尾部空格，写回时零填充固定槽；多列字符串才保留必要全角空格。
- 若译文引入当前码表没有的新字，必须追加码表并重建字体文件，不能只写 overlay。

## 当前测试产物

```text
tools/build_full_writeback_menu_overlay_rom.py
rom/narutorpg3_chs_full_writeback_menu_v1.nds
rom/unpacked/narutorpg3_chs_full_writeback_menu_v1/
plan/cache/text-writeback-smoke/full-writeback-menu-v1-records.json
```

写入内容：

```text
text_writeback_rows=5863
menu_writeback_rows=278
font_cache_strategy=immediate_copy_on_1x2_miss_probe
```

`ndstool -i` 已通过 Header CRC OK / Banner CRC OK。

## 后续验证重点

- 标题菜单和声音测试菜单。
- 存档流程：无数据、损坏数据、文件名、开始游戏入口。
- 战斗菜单和战斗状态提示。
- 道具、装备、状态、队伍、Wi-Fi/好友相关菜单。
- 多列排版字符串是否错位。

可见 UI 以 Windows 窗口截图为准；MCP 优先用于内存、寄存器、断点和加载状态观察。
